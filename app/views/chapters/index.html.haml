.novel
  - if @chapters.any?
    - @chapters.each do |chapter|
      %article
        .title= "#{chapter.id}: #{chapter.title}"
        .body= simple_format chapter.body
        %time= chapter.created_at.strftime('%B %d, %Y')
  - else
    No chapters.
