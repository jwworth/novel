%article
  .title= link_to "#{chapter.id}: #{chapter.title}", chapter_path(chapter)
  .body= simple_format chapter.body
  %time= link_to chapter.created_at.strftime('%B %e, %Y'), chapter_path(chapter)
